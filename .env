# Leave empty so all API calls use relative paths (/api/...) 
# and are intercepted by the Vite dev proxy → http://13.233.207.68:8000
# For production deployment, set this to the full backend URL.

VITE_FINSIGHT_ORIGIN=http://localhost:5173
VITE_PAYABLES_ORIGIN=http://localhost:5174